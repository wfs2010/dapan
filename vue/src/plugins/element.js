import Vue from 'vue'
import { Card, Button, Row, Col, Menu, MenuItem, RadioButton, RadioGroup, DatePicker, Table, TableColumn, Pagination, Input, Select, Option } from 'element-ui'

Vue.use(Button).use(Card).use(Row).use(Col).use(Menu).use(MenuItem).use(RadioButton).use(RadioGroup).use(DatePicker).use(Table).use(TableColumn).use(Pagination).use(Input).use(Select)
.use(Option)
